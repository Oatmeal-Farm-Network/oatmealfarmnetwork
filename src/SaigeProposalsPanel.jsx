/**
 * HITL write approvals happen in chat (reply yes/no to Saige).
 * This panel is intentionally hidden — backend HITL still gates writes.
 */
export default function SaigeProposalsPanel() {
  return null;
}
